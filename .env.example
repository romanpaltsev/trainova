# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,web
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# База данных (внутри docker compose хост — db, локально — localhost)
DATABASE_URL=postgres://trainova:trainova@db:5432/trainova
POSTGRES_DB=trainova
POSTGRES_USER=trainova
POSTGRES_PASSWORD=trainova

# Почта: в разработке — mailpit (UI на http://localhost:8025)
EMAIL_URL=smtp://mailpit:1025
DEFAULT_FROM_EMAIL=Дневник тренировок <no-reply@dnevnik.local>
